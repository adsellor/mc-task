import { StyleSheet } from 'react-native';

const avatarPickerStyles = StyleSheet.create({
    image: {
        borderRadius: 100,
        width: 200,
        height: 200
    }
})

export default avatarPickerStyles;
